# ToolDelta-Web 离线依赖清单（随项目分发）
# 初始化时优先从 ./wheels 本地安装，无需联网，避免共享服务器请求限流导致安装失败。
# 安装命令：pip install --no-index --find-links ./wheels -r ./wheels/requirements.txt
# 说明：仅列出顶层依赖，其传递依赖（multidict/yarl/werkzeug 等）的 wheel 也已一并打包在 ./wheels。
flask==3.1.3
flask-socketio==5.6.1
requests==2.34.2
colorama==0.4.6
pyspeedtest==1.2.7
aiohttp==3.14.3
websocket-client==1.9.0
pyyaml==6.0.3
brotli==1.2.0
websockets==16.1.1
tqdm==4.69.0
anyio==4.14.2
numpy==2.5.1
protobuf==6.31.1
grpcio==1.82.1
msgpack==1.2.1
nbtlib==2.0.4
rich==15.0.0
